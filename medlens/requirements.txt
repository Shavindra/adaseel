# MEDLENS dependencies.
#
# Always needed:
requests>=2.28
pillow>=9.0          # renders the synthetic sample image

# OCR stack (Stage 1, local). Heavy (pulls in torch + downloads models on first
# use). If absent, MEDLENS falls back to a text transcript so the pipeline still
# runs for testing.
docling>=2.0
surya-ocr>=0.6

# Reasoning stage (Stage 3) is vendor-agnostic: it calls any OpenAI-compatible
# /chat/completions endpoint over plain HTTP via `requests` above. No vendor SDK
# (openai/anthropic/etc.) is needed — point --base-url at Ollama, Groq, Gemini, …
