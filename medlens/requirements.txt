# Compatibility installer generated from the core dependency ranges in pyproject.toml.
# Prefer: python -m pip install -e ".[dev]"
requests>=2.31,<3
pillow>=10,<13
typer>=0.12,<1
rich>=13,<15

# Image/PDF OCR is optional and intentionally not part of ordinary installation:
#   python -m pip install -e ".[ocr]"
